name: minimax
version: 1.0.0
description: "MiniMax image generation backend (image-01). Supports text-to-image via the MiniMax API."
author: NousResearch
kind: backend
requires_env:
  - MINIMAX_API_KEY
